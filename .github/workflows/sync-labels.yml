---
name: Sync labels

# yamllint disable-line rule:truthy
on:
  push:
    branches:
      - main
    paths:
      - .github/labels.yml
  schedule:
    - cron: "34 5 * * *"
  workflow_call:
  workflow_dispatch:

permissions:
  issues: write

jobs:
  SyncLabels:
    uses: ivuorinen/ivuorinen/.github/workflows/sync-labels.yml@12541aa4ea9228f2e118f879a66a4a7b402e8ef1 # main
