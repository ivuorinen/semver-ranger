# README Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Write a complete `README.md` at the repo root covering both user-facing and contributor-facing content.

**Architecture:** Single file, Option A structure — user content first (badges, what it does, install, usage, example output), contributor content appended (how it works, dev setup, architecture). No emoji. Pleasant, direct tone. Present tense throughout.

**Tech Stack:** Markdown only. No tooling changes required.

---

## File Map

| Action | Path        | Purpose             |
| ------ | ----------- | ------------------- |
| Create | `README.md` | The complete README |

---

### Task 1: Badges and title block

**Files:**

- Create: `README.md`

- [ ] **Step 1: Write the badges row and title**

The badges use shields.io. Write the following as the first content in `README.md`:

```markdown
[![npm version](https://img.shields.io/npm/v/@ivuorinen/semver-ranger)](https://www.npmjs.com/package/@ivuorinen/semver-ranger) [![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)](LICENSE) [![Node.js](https://img.shields.io/node/v/@ivuorinen/semver-ranger)](https://nodejs.org)

# semver-ranger

Find the safe Node.js and peer dependency version ranges across your entire dependency tree.
```

- [ ] **Step 2: Verify file created correctly**

Run: `head -10 README.md`

Expected: The badges line, blank line, title, blank line, tagline.

- [ ] **Step 3: Commit**

```bash
git add README.md
git commit -m "docs: add README title and badges"
```

---

### Task 2: What it does and why it's useful

**Files:**

- Modify: `README.md`

- [ ] **Step 1: Append the description sections**

Append to `README.md`:

```markdown
## What it does

`semver-ranger` reads your lockfile, fetches `engines` and `peerDependencies` metadata for every resolved package — from the npm registry or local `node_modules` — and computes the semver intersection of all constraints across your full dependency tree. It surfaces conflicts where no safe range exists, and compares your installed versions against the latest available ones.

Supported lockfile formats: `package-lock.json` (npm), `yarn.lock` (Yarn classic and Berry), and `pnpm-lock.yaml` (pnpm). The lockfile is auto-detected from the current directory if no path is provided.

## Why it's useful

Tracing transitive `engines` and peer dependency constraints by hand is impractical once a project has more than a handful of dependencies. `semver-ranger` does it in one command, making it useful for deciding which Node.js version to target, validating that a new package won't break your peer dependency setup, and running constraint checks in CI.
```

- [ ] **Step 2: Commit**

```bash
git add README.md
git commit -m "docs: add what it does and why it's useful sections"
```

---

### Task 3: Install and usage

**Files:**

- Modify: `README.md`

- [ ] **Step 1: Append install and usage sections**

Append to `README.md`:

````markdown
## Install

The quickest way is to run it without installing:

```sh
npx @ivuorinen/semver-ranger
```
````

For frequent use, install globally:

```sh
npm install -g @ivuorinen/semver-ranger
```

## Usage

```sh
semver-ranger [lockfile-path] [options]
```

Run from your project directory to auto-detect the lockfile, or pass a path explicitly.

| Option          | Description                                              |
| --------------- | -------------------------------------------------------- |
| `--offline`     | Skip registry lookups; use `node_modules` and cache only |
| `--check <pkg>` | Add a package to peer dependency analysis (repeatable)   |
| `--no-dev`      | Exclude devDependencies from analysis                    |
| `--all`         | Show all packages, including those with no constraints   |
| `--json`        | Output raw JSON instead of tables                        |
| `--version`     | Print version and exit                                   |
| `--help`        | Print usage and exit                                     |

Exit codes: `0` success, `1` unrecoverable error.

````

(Note: the triple-backtick code fence for the sh blocks must be correct markdown — close the outer block properly.)

- [ ] **Step 2: Commit**

```bash
git add README.md
git commit -m "docs: add install and usage sections"
````

---

### Task 4: Example output

**Files:**

- Modify: `README.md`

- [ ] **Step 1: Append the example output section**

The output format (from `src/output/table.ts`) is:

- Title line, then a `─` separator
- Per target: header with package count, then a table of Package / Installed / Latest / Range columns
- Safe range summary rows
- Conflict rows prefixed with `⚠`

Append to `README.md`:

````markdown
## Example output

```
semver-ranger — package-lock.json (npm) — 214 packages analyzed
────────────────────────────────────────────────────────────────────────────────
node  (engines)  ·  6 packages declare a constraint

  Package                      Installed    Latest       Range
  typescript                   5.4.5        5.6.3        >=4.7
  tsx                          4.19.2       4.19.2       >=18.0.0
  esbuild                      0.21.5       0.24.0       >=12.0.0
  flat-cache                   6.0.0        6.1.4        >=18
  env-paths                    4.0.0        4.0.0        >=18
  semver                       7.6.3        7.6.3        >=10.0.0

  Safe range (installed):       >=18
  Safe range (latest):          >=18

────────────────────────────────────────────────────────────────────────────────
react  (peerDependencies)  ·  3 packages declare a constraint

  Package                      Installed    Latest       Range
  @testing-library/react       16.0.0       16.3.0       ^18.0.0 || ^19.0.0
  react-dom                    18.3.1       19.1.0       ^18 || ^19
  some-legacy-lib              2.4.1        3.0.0        ^16 || ^17

  Safe range (installed):       ⚠  conflict — no safe range
  Safe range (latest):          ⚠  conflict — no safe range

  ⚠  Conflicts at latest (1 package(s) block upgrade):
  ⚠  some-legacy-lib           2.4.1        3.0.0        ^16 || ^17
```
````

- [ ] **Step 2: Commit**

```bash
git add README.md
git commit -m "docs: add example output section"
```

---

### Task 5: How it works (contributor half begins)

**Files:**

- Modify: `README.md`

- [ ] **Step 1: Append the how it works section**

Append to `README.md`:

```markdown
---

## How it works

`semver-ranger` runs a four-step pipeline. First, it detects or accepts a lockfile path and parses all resolved packages from it. Second, it resolves `engines` and `peerDependencies` metadata for each package — checking local `node_modules` first, then querying the npm registry (responses are stored in a flat-file cache so repeated runs are fast). Third, it computes the semver intersection of all constraints for each analysis target (the Node.js engine, or a specific peer package). Finally, it renders the result as an ASCII table or JSON.
```

- [ ] **Step 2: Commit**

```bash
git add README.md
git commit -m "docs: add how it works section"
```

---

### Task 6: Requirements and development setup

**Files:**

- Modify: `README.md`

- [ ] **Step 1: Append requirements and development sections**

Append to `README.md`:

````markdown
## Requirements

- Node.js 22 or later
- `nvm` is recommended to activate the correct version: `nvm use`

## Development

Clone the repository and install dependencies:

```sh
git clone https://github.com/ivuorinen/semver-ranger.git
cd semver-ranger
npm install
```
````

| Command         | Description                                  |
| --------------- | -------------------------------------------- |
| `npm run build` | Compile TypeScript to `dist/` via tsup (ESM) |
| `npm run dev`   | Watch mode compilation                       |
| `npm run lint`  | Prettier format + ESLint auto-fix            |
| `npm run test`  | Run tests with Node's native test runner     |
| `npm run cov`   | Test coverage (experimental)                 |

````

- [ ] **Step 2: Commit**

```bash
git add README.md
git commit -m "docs: add requirements and development sections"
````

---

### Task 7: Architecture and license

**Files:**

- Modify: `README.md`

- [ ] **Step 1: Append architecture and license sections**

Append to `README.md`:

```markdown
## Architecture
```

src/ cli.ts Entry point — argument parsing and pipeline orchestration types.ts Shared TypeScript type definitions analyzer/ Semver intersection logic for engines and peer constraints parsers/ Lockfile parsers: npm, yarn-classic, yarn-berry, pnpm, auto-detect registry/ npm registry client and local node_modules fallback graph/ Dependency graph traversal and devDependency filtering cache/ flat-cache wrapper for registry responses output/ CLI table and JSON rendering

```

Tests live in `test/`, mirroring the `src/` structure. Fixtures for each lockfile format
are in `test/fixtures/`. The test runner is Node's built-in `node --test` with `tsx/esm`
for TypeScript support — no Jest or Vitest.

## License

MIT — [Ismo Vuorinen](https://github.com/ivuorinen)
```

- [ ] **Step 2: Commit**

```bash
git add README.md
git commit -m "docs: add architecture and license sections"
```

---

### Task 8: Final review pass

**Files:**

- Modify: `README.md` (if any corrections needed)

- [ ] **Step 1: Read the full README**

Run: `cat README.md`

Check for:

- All code fences are correctly opened and closed
- No broken markdown (unclosed bold, stray asterisks, etc.)
- The flag table renders cleanly (pipe alignment)
- The example output block is inside a single fenced code block
- `npx` appears before `npm install -g`
- No emoji anywhere
- Tone is consistent — present tense, direct, no marketing language

- [ ] **Step 2: Run the linter**

```bash
npm run lint
```

Expected: no errors. If markdownlint reports issues, fix them.

- [ ] **Step 3: Commit any corrections**

```bash
git add README.md
git commit -m "docs: fix README lint and formatting issues"
```

Only commit if there are actual changes. Skip if clean.
